# ASHBOUND: Return by Ruin

OUT CORE minimal standalone data-driven engine/game prototype.

Run:

```bash
dotnet run
```

Core law:

```text
Input -> OUT_Command -> OUT_Effect / OUT_Event -> OUT_Scheduler -> OUT_Validator -> OUT_Renderer
```

This repository is intentionally built around OUT_ prefixes and OUT CORE philosophy.
